# Iagon MCP Server Configuration

# Required: Your Iagon access token
# Generate at: https://app.iagon.com → Settings → Generate Token
IAGON_ACCESS_TOKEN=your-token-here

# Optional: Transport mode (default: stdio)
# Options: stdio | http
IAGON_TRANSPORT=stdio

# Optional: HTTP server port (only used when IAGON_TRANSPORT=http)
PORT=3000
